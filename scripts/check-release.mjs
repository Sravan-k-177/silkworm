import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {createHash} from 'node:crypto';
const meta=JSON.parse(readFileSync('public/models/larval-appearance.json','utf8'));
const hash=createHash('sha256').update(readFileSync('public/models/larval-appearance.onnx')).digest('hex');
const en=JSON.parse(readFileSync('src/locales/en.json','utf8')),te=JSON.parse(readFileSync('src/locales/te.json','utf8'));
const report={checkedAt:new Date().toISOString(),checks:{modelChecksumMatches:hash===meta.report.onnx_sha256,teluguKeysComplete:Object.keys(en).every(k=>typeof te[k]==='string'&&te[k].trim()),teluguFontBundled:existsSync('public/fonts/NotoSansTelugu.ttf'),httpsConfig:existsSync('deployment/Caddyfile'),accountServer:existsSync('server/auth.ts'),externalEvaluator:existsSync('ml/evaluate_external.py')},externalEvidence:{departmentalDataset:'Official link unavailable at last access check; no verified download',independentFarmAccuracy:'Not established: independent labelled data not supplied',earlyWarningLeadTime:'Not established: independently timed onset cohort not supplied',yieldImprovement:'Not established: outcome impact study not supplied',reviewedTeluguCare:'Software translation implemented; independent language/SOP review not obtained',publicDeployment:'Configuration prepared; domain and host access required'},excludedByUser:['Physical phone testing','Real shed testing']};
writeFileSync('artifacts/release-readiness.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(Object.values(report.checks).some(v=>!v))process.exitCode=1;
